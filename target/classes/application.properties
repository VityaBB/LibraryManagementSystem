# Database Configuration
db.url=jdbc:postgresql://localhost:5432/library_db
db.user=library_user
db.password=123456
db.driver=org.postgresql.Driver

# Application Settings
app.name=Library Management System - JDBC
app.version=1.0.0

# Pagination
app.default.pageSize=10
app.max.pageSize=100

# Logging
app.debug=true